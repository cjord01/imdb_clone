<h2>
	<%= @movie.title %>
	<%=" (#{@movie.year})"%>
</h2>

<h3>
	<% @roles.each do |role| %>
		<%= "#{role.name} - " %>
		<%= link_to "#{role.actor.name}", actor_path(role.actor) %>
		<br>
	<% end %>
</h3>